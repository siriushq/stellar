siriushq/stellar.configuration
==============================

Provides external configuration for JVM daemon services, by default sourcing
from JVM Properties, environment variables, and .properties files, also
providing dynamic hot-reload functionality using explicit bindings.