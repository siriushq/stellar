<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.siriushq.stellar</groupId>
        <artifactId>stellar</artifactId>
        <version>1.0-RC1</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <packaging>jar</packaging>
    <artifactId>stellar-serialization-msgpack</artifactId>
    <name>Stellar :: Serialization :: MessagePack</name>

	<licenses>
        <license>
            <name>BSD 3-Clause License</name>
            <url>https://opensource.org/license/bsd-3-clause</url>
        </license>
        <license>
            <name>Apache 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

	<properties>
		<java.version.main>11</java.version.main>
		<java.version.test>11</java.version.test>
	</properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>

				<executions>
					<execution>
						<id>default-compile</id>
						<configuration combine.self="override">
							<release combine.self="override"/>
							<source>${java.version.main}</source>
							<target>${java.version.main}</target>
							<proc>full</proc>

							<compilerArgs>
								<arg>--add-exports</arg>
								<arg>java.base/sun.nio.ch=sirius.stellar.serialization.msgpack</arg>
							</compilerArgs>
						</configuration>
					</execution>
				</executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>

				<configuration>
					<additionalOptions>
						<additionalOption>--add-exports</additionalOption>
						<additionalOption>java.base/sun.nio.ch=sirius.stellar.serialization.msgpack</additionalOption>
					</additionalOptions>
				</configuration>
            </plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>

				<configuration>
					<argLine>
						--illegal-access=permit
						--add-opens java.base/java.nio=sirius.stellar.serialization.msgpack
						--add-opens java.base/sun.nio.ch=sirius.stellar.serialization.msgpack
					</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-gpg-plugin</artifactId>
			</plugin>
        </plugins>
    </build>
</project>